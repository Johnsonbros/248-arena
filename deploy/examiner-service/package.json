{
  "name": "arena-examiner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Examiner — grounded AI study tutor for 248 Arena (text mode). Retrieval over the app's own 248 CMR content, generation via the fleet's LLM gateway.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
